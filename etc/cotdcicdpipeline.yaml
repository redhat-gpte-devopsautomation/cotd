apiVersion: v1
kind: BuildConfig
metadata:
  name: cotdcicdpipeline
  labels:
    name: cotdcicdpipeline
  annotations:
    pipeline.alpha.openshift.io/uses: '[{"name": "cotdcicd", "namespace": "", "kind": "DeploymentConfig"}]'
spec:
  triggers:
    -
      type: GitHub
      github:
        secret: secret101
    -
      type: Generic
      generic:
        secret: secret101
  runPolicy: Serial
  source:
    type: None
  strategy:
    type: JenkinsPipeline
    jenkinsPipelineStrategy:
      jenkinsfile: "node('maven') {\n\
      \nstage '==> Build-In-Development'\n\ 
      \nopenshiftBuild(namespace: 'development', buildConfig: 'cats', showBuildLogs: 'true')\
      \nopenshiftBuild(namespace: 'development', buildConfig: 'cities', showBuildLogs: 'true')\
      \n\nstage '==> Deploy-In-Development'\n\
      \nopenshiftDeploy(namespace: 'development', deploymentConfig: 'cats')\ 
      \nopenshiftDeploy(namespace: 'development', deploymentConfig: 'cities')\
      \n\nstage '==> Tag-Image-in-Development'\n\
      \nopenshiftTag(namespace: 'development', sourceStream: 'cats', sourceTag: 'latest', destinationStream: 'cats', destinationTag: 'promoteToQA')\
      \nopenshiftTag(namespace: 'development', sourceStream: 'cities', sourceTag: 'latest', destinationStream: 'cities', destinationTag: 'promoteToQA')\
      \n\nstage '==> Deploy-In-Testing' \n\
      \nopenshiftDeploy(namespace: 'testing', deploymentConfig: 'cats', )\
      \nopenshiftScale(namespace: 'testing', deploymentConfig: 'cats', replicaCount: '2')\
      \n\n}\n"
  output:
  resources:
  postCommit:
